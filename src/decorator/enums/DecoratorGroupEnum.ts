export enum DecoratorGroupEnum {
  COMMONS = 'commons',
  MODULES = 'modules',
  PROVIDERS = 'providers',
  CONSUMERS = 'consumers',
  VALIDATIONS = 'validations',
}

export default DecoratorGroupEnum;
